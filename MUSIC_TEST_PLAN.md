# 初音未来音乐播放器验收测试清单

## 资源验收

- 运行 `npm test` 检查播放器结构、曲库数量、必含曲目、默认曲目、控制函数、合法来源链接和当前已接入音源。
- 运行 `npm run test:music-assets` 做严格音源验收；缺少任意曲目音频时，该命令必须失败。
- 曲库数量保持在 10 到 16 首之间，当前版本为 10 首。
- 必含《世界第一的公主殿下》《妄想感伤代偿连盟》《千本樱》。
- 每首歌的 `vocal` 必须标记为初音未来或 Hatsune Miku。
- 音频来源必须可授权使用，并保留购买或授权记录。
- 优先使用 FLAC/WAV；没有合法无损来源时再使用高码率 MP3。
- 除现有 `assets/bgm.mp3` 外，其余曲目按 `assets/audio/<filename>.flac|.wav|.mp3` 自动尝试加载。
- 音频文件需要能正常加载、播放、读取时长，不能是空文件或损坏文件。

## 播放器窗口

- 点击右下角黑胶唱片图标，能弹出播放器窗口。
- 点击播放器窗口外部区域，窗口能自动缩回。
- 缩回后当前播放状态、当前曲目和播放进度不被重置。
- 移动端和桌面端窗口不遮挡核心内容，控制区不重叠。

## 播放控制

- 播放、暂停、上一首、下一首均可用。
- 顺序模式按列表顺序切歌，末尾回到第一首。
- 随机模式不固定顺序，并避免连续重复当前歌曲。
- 拖动进度条后能跳转到目标时间。
- 快速连续切歌时，不出现两首音频同时播放。

## 显示同步

- 播放器内能看到全部曲名。
- 当前歌曲在曲库列表中高亮。
- 切歌后窗口标题、右下角歌名、状态文案同步更新。
- 每首歌对应的黑胶唱片封面能正确切换。
- 音源缺失或加载失败时有明确提示，并显示期望文件名。
